terraform {
  cloud {
    organization = "nolanwelch"
    workspaces {
      name = "github-actions"
    }
  }

  required_providers {
    cloudflare = {
      source  = "cloudflare/cloudflare"
      version = "~> 4.0"
    }
  }
}

provider "cloudflare" {
  api_token = var.cloudflare_api_token
}
